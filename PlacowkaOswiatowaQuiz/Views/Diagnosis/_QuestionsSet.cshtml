@model QuestionsSetViewModel

<br />
<div class="container">
    <div id="divAlertArea">
        <partial name="~/Views/UIComponents/_bootstrapAlert.cshtml" />
    </div>
    <div class="container rounded p-2">
        <div class="row">
            <div class="col-12 col-lg-6 pb-4">
                <h4 class="d-flex justify-content-between align-items-center mb-3">
                    Zestaw pytań:
                </h4>
                <div class="row my-1">
                    <div class="col-3">
                        <label>Name</label>
                    </div>
                    <div class="col-9">
                        <input placeholder="Nazwa" class="form-control" />
                        <span class="text-danger"></span>
                    </div>
                </div>
                <div class="row my-1">
                    <div class="col-3">
                        <label>Phone</label>
                    </div>
                    <div class="col-9">
                        <input placeholder="Telefon" class="form-control" />
                        <span class="text-danger"></span>
                    </div>
                </div>
            </div>
            <div class="col-12 col-lg-5 offset-lg-1">
                <h4 class="d-flex justify-content-between align-items-center mb-3">
                    <span class="text">Pytania:</span>
                </h4>
                <ul class="list-group mb-3">
                    @foreach (var question in Model.Questions)
                    {
                        <li class="list-group-item d-flex justify-content-between">
                            <div>
                                <input type="radio" name="isAnswered" value="" />
                                <h6 class="my-0">@question.Content</h6>
                                <small class="text-muted">@question.Description</small>
                            </div>
                            @*<span class="text-muted">@((details.Price * details.Count).ToString("c")) </span>*@
                        </li>
                    }
                    @*<li class="list-group-item d-flex justify-content-between bg-light">
                            <small class="text-info">Total (USD)</small>
                            <strong class="text-info">Podsumowanie</strong>
                        </li>*@
                </ul>
            </div>
        </div>
    </div>
    @*<div class="card-footer">
            <div class="row">
                <div class="col-12 col-md-8 pt-2">
                    <p style="color:maroon; font-size:14px;">
                        Estimate Arrival Date:
                        @DateTime.Now.AddDays(7).ToShortDateString() - @DateTime.Now.AddDays(14).ToShortDateString()
                    </p>
                </div>
                <div class="col-12 col-md-4">
                    <button type="submit" value="Place Order" class="btn btn-primary form-control">Place Order</button>
                </div>
            </div>
        </div>*@
</div>

