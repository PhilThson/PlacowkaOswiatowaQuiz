@model string[]

<div>
    <table style="width:100%">
        <tbody>
            @for (int i = 0; i < Model.Count(); i++)
            {
                <tr>
                    <td>@Html.DisplayFor(m => m[i])</td>
                </tr>
            }
            </tbody>
    </table>
</div>